# 自走改善ログ（2026-07-15 夜間）

オーナー指示により、既存TODO完了後にアプリ全体の改善点・バグ・磨き込みを自分で見つけて自走で
改善する。各サイクルは「小さく・テスト付き・可逆・CIグリーンでのみマージ」を原則とし、
リスクの高いリファクタや破壊的操作は行わない。本番URL: https://meta-geo-poker.vercel.app

## 進め方
1サイクル = 1改善: 実装 → typecheck/関連テスト → バージョン+1 → commit → PR → CIグリーンでマージ →
Deploy成功確認 → 本ログに追記 → 次サイクルを予約。朝(JST）に総括レポートをチャットへ。

## バックログ（優先度順・随時更新）
- [ ] ローディングの「読み込み中…」テキストを、上質なスケルトンローダーへ置換（Stats/History/Leaderboard/GEO）
- [x] Stats等の主要数値にカウントアップ演出（結果画面のuseCountUpを共通化）
- [x] 各タブの空状態（データ0件）を、気の利いたコピー＋アイコンで丁寧に
- [x] キーボードフォーカス可視化（focus-visible）とタップ領域の見直し（a11y）
- [ ] GEO集計 fetchRawHands のTTLメモリキャッシュ（無駄な全件再取得の抑制）
- [x] サーバーの死にコード整理（disconnectTimer 周辺の簡素化）
- [x] エンジン/サーバーのルール網羅レビュー（サイドポット・アンテ・復帰の端）
- [x] 画面遷移・ボタンのマイクロインタラクション微調整

## 変更ログ
- **v1.74.0 — スケルトンローダー導入**: 「読み込み中…」テキストを、ink-200パルスの上質な
  スケルトン（Skeleton / ChartSkeleton / ListSkeleton）へ置換。Statsのグラフ読み込み、
  Leaderboard/History/Tournamentsの一覧読み込み、Statsタブ全体の読み込みに適用。体感速度と
  完成度を向上。
- **v1.75.0 — 主要数値のカウントアップ演出**: 結果画面のuseCountUpを `@/lib/useCountUp` へ共通化し、
  Statsの収支/ROI/かけた金額/得た金額/参加トナメ数/インマネ回数・率/VPIP/PFR/3Betが 0→実値へ
  easeOutCubicでカウントアップするように。数字が動く上質な体験に。
- **v1.76.0 — a11y: フォーカス可視化 + reduced-motion**: キーボード操作時のみ ink-950 の実線
  フォーカスリングを出す `:focus-visible` を全インタラクティブ要素に統一適用。`prefers-reduced-motion`
  を尊重し、アニメーション/トランジションを実質無効化(乗り物酔い配慮)。
- **v1.77.0 — 空状態(EmptyState)の作り込み**: モノクロSVGアイコン＋太字見出し＋補足コピーの
  共通 EmptyState を新設し、Leaderboard(ランキング0件)/History(ハンド0件・お気に入り0件)/
  Tournaments(成績0件)の素っ気ないテキストを置換。新規ユーザーの第一印象を向上。
- **v1.78.0 — 死にコード整理**: 切断で自動離脱させない仕様変更(v1.66.0)以降、未設定のまま
  残っていた `disconnectTimer`(HumanSeat/HumanEntry のフィールド・初期化・attachHumanの
  クリア処理)をSNG/MTT両方から除去。挙動不変・保守性向上。engine 71 / server 13 テスト green。
- **v1.79.0 — マイクロインタラクション**: トーナメント履歴カード(タップで詳細モーダル)に
  whileTap の押し込み(scale 0.985)を追加し、タップの触感フィードバックを付与。
- **v1.80.0 — ルールレビュー + 設定メニューの触感**: エンジンの `bettingLogic.ts`(TDA Rule 47:
  フル/不完全レイズの再オープン判定)を精査し健全であることを確認。あわせてプレイ中の設定
  ポップオーバー各項目に active:scale の押し込みを追加。ロジック不変。
- **v1.81.0 — タイポグラフィのアンチエイリアス**: body に -webkit-font-smoothing:antialiased /
  -moz-osx-font-smoothing:grayscale / text-rendering:optimizeLegibility を追加し、全画面の文字を
  くっきり端正に(Swiss/Apple寄りの見え方を後押し)。CSSのみ。
- **v1.85.0 — 細身で控えめなスクロールバー**: 太いOSデフォルトのスクロールバーを避け、
  `scrollbar-width:thin` と ink系の半透明サム(hoverで濃くなる)に統一。デスクトップや
  スクロールバー常時表示環境でアプリのトーンに馴染ませる(モバイルのオーバーレイ表示には影響なし)。CSSのみ。
- **v1.87.0 — プレイ中UI磨き(平均スタック/吹き出し/メニュー整理)**: (1)左上タイマーに「Ave : NBB」形式で
  トーナメント平均スタックをBB表記で追加。(2)発言吹き出しを黒縁の武骨なデザインから、ヘアライン+
  柔らかいドロップシャドウ+菱形の尻尾でカードの真上にふわっと浮かぶApple的な吹き出しへ刷新
  (静的モックでスクショ3回検証)。(3)ホームのハンバーガーメニューから一貫性の無い
  「ブラインドストラクチャ」「GEOデータベース」項目を削除。
- **v1.88.0 — チャットログから発言可能に+発言者表示**: 設定→チャットログの最下部に入力欄と
  送信ボタンを追加し、そこからも発言できるように(自席の吹き出しにも反映)。各メッセージの
  吹き出し横に発言者のアイコンと名前を表示(LINE風。自分は右・黒、相手は左・白+黒枠)。
- **v1.89.0 — アクションボタンをペルソナ5風に**: プレイ中の主要アクションボタン(Fold/Check/Call/Raise)を、
  斜めに歪んだ平行四辺形+黒のハードなオフセット影+太いイタリック体の攻めたデザインへ刷新。
  意味の色分け(青=Fold/緑=Check・Call/赤=Raise・All In)は維持。静的モックでスクショ検証。
- **v1.90.0 — オールイン炎エフェクトを本物の炎に**: 従来の放射状ブラー3枚だけの簡素な炎を、
  高さ違い7本の火柱(位相のずれた揺らぎアニメ)+暖色グロー+立ち上る火の粉で構成した
  リッチな炎へ刷新。アバターの背面に置きプレイヤーの顔を隠さず、背後〜側面〜頭上へ舐め上がる。
  背景色に依存しないよう不透明グラデ+drop-shadowで描画(明/暗どちらの背景でも破綻しないことを
  静的モックで検証)。CSSキーフレーム(allin-flick/glow/ember)で軽量に実装。

## 自走改善#2(プレイ中の一貫性・Apple品質)
- **v1.91.0 — 手番待ちボタンをP5と同じ平行四辺形に統一**: 手番待ち中の x/f・離席・プレースホルダを、
  アクティブ時のペルソナ5ボタンと同じ平行四辺形シルエットに統一(色塗り・オフセット影は省いた控えめな
  枠線のみ)。手番が来ると同じ形が色付きP5ボタンへ“起動”する、一貫したデザイン言語に。
- **v1.92.0 — チャット送信ボタンの一貫性**: プレイ中のクイックチャット入力バーの送信ボタンを、
  チャットログの送信ボタンと同じ黒丸+矢印アイコンに統一(テキスト「送信」から変更)。
- **v1.93.0 — ログインのモバイル入力体験を改善**: メール欄に autoCapitalize=none / autoCorrect=off /
  spellCheck=false を付与し、モバイルでメールアドレスが勝手に大文字化・自動修正されないように。
  各入力欄に enterKeyHint(メール=next/send, パスワード=go/next)を付け、ソフトキーボードの
  Enterキーの意味を明確化。表示/入力補助のみでロジック不変。
