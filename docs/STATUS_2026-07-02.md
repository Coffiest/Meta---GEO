# 進捗報告(2026-07-02)

自走で進めた内容のまとめです。PR: https://github.com/Coffiest/Meta---GEO/pull/1(Draft)

## ブロッカー(私の判断では解決できなかったこと)

- **`naoyuki/GEO_Card` のカードデザインを取得できませんでした**。このセッションのGitHubアクセスは
  `coffiest/meta---geo` のみに制限されており、他リポジトリへのアクセスは拒否される仕様です
  (`Access denied: repository "naoyuki/geo_card" is not configured for this session`)。
  → **画像を差し込むだけで反映される仕組みは用意済み**です(下記参照)。このリポジトリに直接
  画像ファイルを置いていただくか、次回セッションでこのリポジトリへのアクセス権を含めて
  `naoyuki/GEO_Card` へのアクセスを許可していただければ、私の側で取り込んで即適用できます。
- **実際の本番デプロイ(Vercel/Fly.io/Supabase)は実行していません**。このセッションにはそれらの
  アカウント認証情報が無いためです。「コマンド一つで実行できる」状態までは整備しました
  (`docs/DEPLOYMENT.md`)。

## 今回追加した内容

### 1. カード・テーブルデザインの差し込みシステム
- `apps/web/public/cards/` に `As.png` `10h.png` `back.png` のような命名規則で画像を置くだけで、
  現在のCSS描画から自動的に切り替わります(画像が無い/読み込み失敗時は現行デザインにフォール
  バックするので、リポジトリの状態を崩さず段階的に差し替え可能)。命名規則・必要枚数は
  `apps/web/public/cards/README.md` に一覧化済み。
- 同様に `apps/web/public/table/felt.png` を置くだけでテーブル背景(フェルト部分)が切り替わります。
  詳細は `apps/web/public/table/README.md`。
- **naoyuki/GEO_Cardのデザインが届き次第、このフォルダに配置するだけで反映されます。**

### 2. GEO戦略分析画面(前回セッションの続き、`/geo`)
ポジション別プリフロップ・オープン頻度チャート、KPIタイル、ハンド履歴一覧+詳細を実装。
dataviz skillの手順(検証済みカラー・凡例・ホバー・テーブル表示フォールバック)に準拠。

### 3. CI(GitHub Actions)
`.github/workflows/ci.yml` を追加。push/PRごとに実Postgresコンテナに対してengine/db/serverの
テストと型チェック、webのビルドを自動実行します。**このPRの2回のプッシュとも実際にCIが
success で完走することを確認済み**です。

### 4. デプロイ準備
- `apps/web/vercel.json`(モノレポ対応ビルドコマンド)
- `packages/server/Dockerfile` + `fly.toml`(Socket.IOサーバー用、常時起動プロセスが必要な
  ためVercelではなくFly.io想定)
- `docs/DEPLOYMENT.md` に手順を明文化(Supabase接続文字列の取得→マイグレーション適用→
  Fly.ioへのデプロイ→Vercelへのデプロイ、の順)
- **注意**: この環境ではDocker-in-Dockerが使えずDockerfileの実ビルド検証ができていません。
  Vercelアカウントでの実デプロイ検証も同様に未実施です。両方とも `docs/DEPLOYMENT.md` に
  正直に明記しています。

### 5. モバイル実機相当の表示確認
320px(iPhone SE相当)〜744px(iPad mini縦持ち相当)の4サイズでヘッドレスブラウザにより
実際にプレイ操作を行い、横スクロール発生なし・JSエラーなしを確認しました。

## 現在のテスト状況

`pnpm -r --filter=./packages/* test` で **59件のテスト全てgreen**(engine 51 / db 2 / server 6)。
CIでも同様に確認済みです。

## 次にやると良さそうなこと(優先度は私の判断です、変更歓迎)

1. **naoyuki/GEO_Card のデザインをこのリポジトリに取り込む**(私からはアクセスできないため、
   このリポジトリへの直接配置、またはアクセス権の付与をお願いします)
2. 完成したポーカーテーブルデザインの適用(`public/table/felt.png` に配置するだけで反映されます)
3. Vercel/Fly.io/Supabaseアカウントでの実デプロイ実行(`docs/DEPLOYMENT.md` の手順通り)
4. サブスク課金(Stripe連携)、日英対応、MTT(複数卓)対応 — まだ着手していません

## 確認方法

- PRの差分: https://github.com/Coffiest/Meta---GEO/pull/1/files
- ソロテスト手順: `docs/SOLO_TESTING.md`(サーバー+DB+Webを起動してBOT対戦・DB記録を確認)
- `/geo` にアクセスすると、これまでの全ハンドの分析画面が見られます
