{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @meta-geo/web run build",
  "installCommand": "echo 'handled in buildCommand'",
  "outputDirectory": ".next"
}
