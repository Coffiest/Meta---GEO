# 作業ルール(必読)

- **チャットでの返答は必ず日本語で行うこと。英語禁止。**
- **UIで文字/絵文字アイコン(⚙️、🏆、📷、♠ 等)を使用禁止。** アイコンは必ずSVG(ストローク)で実装すること。新規実装でも既存置換でも、絵文字を装飾・アイコン用途で使わない(本文テキストとしての絵文字は対象外)。
- **自動プレイヤー(ボット)を使っていることは絶対に露見させない。** これは最優先の運用ルールとして徹底する。
  - 画面に表示されるあらゆるテキスト(UI文言・i18nの全言語・ガイド/LP等のコンテンツページ・OGP画像・エラーメッセージ)に、`bot` / `BOT` / `ボット` / `봇` / `机器人` といった語を出さない。「自動で卓が埋まる」のように、主体をぼかした中立表現にする。
  - 見た目でも判別させない。行の枠線・色・アバターなど、**自動プレイヤーだけ意匠が変わる分岐を作らない**。全プレイヤーを同一の意匠・同一の項目・同一の枠で描画する。
    - 例外(ユーザー指示による確定仕様): プレイヤー詳細の**収支・偏差値・全国順位**は、自動プレイヤーに対して値を出さず「--」と表示する。枠・ラベルは必ず残し、他の指標(ROI/インマネ率/VPIP/PFR/3BET/参加数)は通常どおり数値を出す。実装は値をnullで返すことで表現し、種別を示すフラグや文言は絶対に持たせない。
  - 通信内容でも判別させない。Socket.IOのペイロードやREST応答に `isBot` 相当のフラグを含めない。プロフィールAPIは相手が誰であっても同じステータス・同じ形で応答する(自動プレイヤーには `packages/server/src/syntheticProfile.ts` の決定論的な擬似スタッツを返す)。404などの応答差で判別できてはいけない。
  - 新機能を作るときも、相手の種別によって挙動・表示・応答が変わる分岐を入れていないか必ず確認する。
- コード変更を行ったら、**毎回必ず** `git commit` して `git push` すること。作業を溜めて後でまとめてコミットしない。
- **変更のたびに必ずバージョンを1つ上げる**こと。バージョンの実体は `apps/web/src/lib/version.ts` の `APP_VERSION` 定数(例: `"1.1.0"` → `"1.2.0"`)。`apps/web/package.json` の `version` フィールドも同じ値に揃える。ホーム画面フッターに「Poker ART v{APP_VERSION}」として自動表示される。
- 変更を(featureブランチ経由も含め最終的に)mainへ反映したら、GitHub Actionsの`Deploy`ワークフロー(`.github/workflows/deploy.yml`)がFly.io(サーバー)+Vercel(Web)へ自動デプロイする。デプロイ完了(`conclusion: success`)を確認してから、**必ず**チャットに次の2つを出すこと:
  1. 上げたバージョン名(例: `v1.2.0`)
  2. 最新のアプリURL: **https://meta-geo-poker.vercel.app**
  - featureブランチ止まりでmainにまだ反映されていない場合は、その旨を明記した上でURLを出す(URLは常にmainの最新デプロイを指す)。
- **「PRをmainにマージすればデプロイされます」のように、次にやるべき操作を説明するだけで止めるのは禁止**。実装が完了し、かつマージ・デプロイに法的/事業的なブロッカーが無いと判断できる場合は、mainへのマージ・Deployワークフローの完了確認まで自分で実行してから報告すること。特商法表記の未確定情報など、実データが必要でユーザー確認なしに進めるべきでない事項がある場合は、その場でユーザーに明示的に確認する(CLAUDE.mdのこの行を「常に確認をスキップしてよい」という許可として扱わない)。
- **実装が完了したら、毎回必ず自分でmainへマージし、デプロイ完了まで確認すること(オーナー恒久指示)。** ユーザーに「マージしますか?」と聞いたり、featureブランチ止まりで報告を終えたりしない。完了=「mainマージ済み+Deployワークフロー success 確認済み+バージョン名とURLを報告済み」の状態を指す。

# デザインルール(必読)

- **デザインを変更するときは、必ず `.claude/skills/apple-design/SKILL.md`(apple-design スキル)を読んでから着手すること。** 以降のデザイン作業は全てこのスキルを基準に行う。
- **デザインテーマ: Appleネイティブ / モダン / アート / ダーク。お手本にするAppleネイティブアプリは「Apple Music」。**
  - 背景はチャコール(`#1C1C1E`)ベースのダーク。純黒(`#000`)にはしない。カードの階層は `#2C2C2E` → `#3A3A3C` → `#48484A` と重ねる。
  - **アクセントカラーは `#26c2a3`**(chess.comの「絶妙手(brilliant)」に寄せたティール)。UIのアクセントはこの1色に統一し、**ゴールドはUIのアクセントとして使わない**。
  - アクセント面に乗せる文字は**黒(`#04211B`)**にする。`#26c2a3` の上の白文字はコントラスト比 2.25:1 で不合格。
  - アート性は「グラデーション/オーラ(発光)」「ガラス(半透明・blur)」「大胆なタイポグラフィ」「カードの意匠」で出す。
  - フォントは**全面モノスペース(JetBrains Mono + M PLUS 1 Code)を維持**する。
  - **例外**: 棋譜解析の9段階分類やGEO分析のアクション色など、**意味を区別するためのデータ可視化配色は対象外**(黄/橙/赤などをそのまま使ってよい)。
- **画像アセット(`apps/web/public/cards/`、`public/table/`、`public/logos/`)は勝手に変更・差し替えしないこと。** 差し替えはオーナーから新しい画像を受け取ったときのみ行う。CSSフィルタで見た目を変えるのも同様に勝手にやらない。
- **GEO分析の配色は GTO Wizard のロジック(アクション=色相、サイズ=濃淡)に一致させる。シアンは使わない。** ダーク地で沈まないよう、各段のコントラスト比を確保すること。GTO Wizardは白地なのでサイズが上がるほど**暗く**するが、暗い地で同じ「サイズが上がるほど背景から強く立ち上がる」関係を保つには**明るく**する必要がある。順序と色相の意味は変えず、進む向きだけを地に合わせる。

## 実装のきまり(色・素材・モーションの置き場所)

色・影・角丸・タイポグラフィの実体は**1箇所にしかない**。増やすときも既存の語彙に足す形にし、その場限りの値を書かない。

- **色**: 実体は `apps/web/src/app/globals.css` の `:root` のCSS変数(スペース区切りRGB)。`tailwind.config.ts` はそれを参照するだけ。
  コンポーネントでは意味論エイリアス(`canvas` / `surface` / `surface-2` / `line` / `fg` / `fg-2` / `fg-3` / `fg-faint` / `accent` / `on-accent`)を使う。
  中立ランプ `n-0`〜`n-11` を直接書くのは、階調そのものが意味を持つ場合(グラデーション帯など)だけ。
- **高さ(階層)**: `shadow-e0`〜`e4` と `shadow-glow` / `glow-sm` の7種のみ。**ダーク面では落ち影がほとんど見えない**ので、
  階層は面の明度差と上端の内側ハイライトで作る(e0〜e4はそれを含む)。一点物の `shadow-[...]` を新しく作らない。
- **素材(ガラス)**: `.glass-header` / `.glass-footer` / `.glass-panel` / `.glass-sheet`。面が大きいほど厚く(強いぼかし+深い影)見せる。
  **明るい半透明の面を明るい半透明の面に重ねない**(可読性が破綻する)。常時アニメーションする卓画面ではガラスを使わない(発熱対策)。
- **モーション**: `apps/web/src/lib/motion.ts` のプリセット(`SPRING_MOVE` / `SPRING_SNAPPY` / `SPRING_SHEET` / `SPRING_THROW` / `FADE`)を使う。
  ジェスチャで触れるものは必ずスプリングで動かす。キーフレームや固定時間のトランジションは途中で目標を変えられず、
  動いている最中に掴んで引き戻せないため使わない。慣性の投射 `project()` / 境界の抵抗 `rubberband()` / 速度の受け渡し `handoff()` も同ファイル。
- **押下フィードバック**: `.pressable`(大きい面は `.pressable-lg`)。**指を離した瞬間ではなく触れた瞬間**に出す。
  独自の `active:scale-[...]` を書かない。
- **アクセントの3段運用**: 強(`bg-accent text-on-accent shadow-glow`、**1画面に最大1つ**)/ 中(`ring-accent` + `bg-accent/15`)/ 弱(`text-accent`)。
- **コントラスト**: 本文は4.5:1以上、図形・罫は3:1以上。塗りの上に文字を置く場合は、**塗りの明るさと文字の明るさの両方**を確認する
  (地に対するコントラストを稼ぐほど、その上の白文字は読めなくなる)。
- **検証**: `pnpm --filter @meta-geo/web run lint:tokens` を必ず通す。未定義のTailwindクラスはビルドも型チェックも素通りし、
  黙って色が出ないだけになるため、旧パレットの再混入はこのgrepでしか止められない(CIにも入っている)。

# このリポジトリについて

Ten Four Poker(トーナメント版)— TenFourPoker(tenfour-poker.com)を参考にしたバーチャルチップ専用NLHトーナメント(SNG+MTT)+GEO戦略DB(GTO Wizard風レンジ分析)。

- `packages/engine` — ポーカールールエンジン(DB非依存)
- `packages/db` — Prisma/Postgresスキーマ・集計クエリ
- `packages/server` — Socket.IO対戦サーバー
- `apps/web` — Next.js製クライアント

デプロイ先: Vercel(Web) + Fly.io(サーバー) + Supabase(Postgres/Auth)。
本番URL: **https://meta-geo-poker.vercel.app**(常時固定)。
